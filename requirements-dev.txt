# Development / testing dependencies for PipeUHR.
# Install with: py -m pip install -r requirements-dev.txt
#
# Includes the runtime requirements so a fresh venv only needs this one file.
-r requirements.txt

pytest>=8.0
pytest-cov>=5.0
pytest-playwright>=0.5
